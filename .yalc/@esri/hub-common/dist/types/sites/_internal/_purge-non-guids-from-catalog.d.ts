import { IModel } from "../../types";
/**
 * Remove any non-guid entries from the data catalog groups array
 * @param {object} model Site Model
 * @private
 */
export declare function _purgeNonGuidsFromCatalog(model: IModel): IModel;
