import { IModel } from "../../types";
/**
 * Apply the first schema version to the item
 * @param {Object} model Site Model
 * @private
 */
export declare function _applySiteSchema(model: IModel): IModel;
